basePath: /api
definitions:
  models.BaseID:
    properties:
      id:
        example: 479b5b5f-81b1-4669-91a5-b5bf69e597c6
        type: string
    type: object
  models.Note:
    properties:
      content:
        type: string
      createdAt:
        example: 2006-01-02T15:04:05Z07:00
        type: string
      deletedAt:
        example: 2006-01-02T15:04:05Z07:00
        format: date
        type: string
      id:
        example: 479b5b5f-81b1-4669-91a5-b5bf69e597c6
        type: string
      title:
        type: string
      updatedAt:
        example: 2006-01-02T15:04:05Z07:00
        type: string
      user:
        $ref: '#/definitions/models.User'
      userId:
        type: string
      visibility:
        $ref: '#/definitions/models.NoteVisibility'
    type: object
  models.NoteVisibility:
    enum:
    - public
    - private
    type: string
    x-enum-varnames:
    - NotePublic
    - NotePrivate
  models.SambaBool:
    enum:
    - "yes"
    - "no"
    type: string
    x-enum-varnames:
    - "YES"
    - "NO"
  models.Share:
    properties:
      adminUsers:
        example:
        - root
        items:
          type: string
        minItems: 1
        type: array
      browsable:
        allOf:
        - $ref: '#/definitions/models.SambaBool'
        example: "yes"
      path:
        example: /var/www/html
        type: string
      permissions:
        example:
        - 7
        - 7
        - 7
        items:
          type: integer
        type: array
      readOnly:
        allOf:
        - $ref: '#/definitions/models.SambaBool'
        example: "no"
      validUsers:
        example:
        - guest
        - chesta
        items:
          type: string
        minItems: 1
        type: array
    required:
    - adminUsers
    - browsable
    - path
    - permissions
    - readOnly
    - validUsers
    type: object
  models.ShareMap:
    additionalProperties:
      type: string
    type: object
  models.Shares:
    additionalProperties:
      $ref: '#/definitions/models.Share'
    type: object
  models.Sort:
    enum:
    - asc
    - desc
    type: string
    x-enum-varnames:
    - ASC
    - DESC
  models.User:
    properties:
      createdAt:
        example: 2006-01-02T15:04:05Z07:00
        type: string
      deletedAt:
        example: 2006-01-02T15:04:05Z07:00
        format: date
        type: string
      email:
        example: chestaardi4@gmail.com
        type: string
      id:
        example: 479b5b5f-81b1-4669-91a5-b5bf69e597c6
        type: string
      status:
        $ref: '#/definitions/models.UserStatus'
      updatedAt:
        example: 2006-01-02T15:04:05Z07:00
        type: string
      username:
        example: chesta_ardiona
        type: string
    type: object
  models.UserStatus:
    enum:
    - active
    - pending
    type: string
    x-enum-varnames:
    - UserActive
    - UserPending
  multipart.FileHeader:
    properties:
      filename:
        type: string
      header:
        $ref: '#/definitions/textproto.MIMEHeader'
      size:
        format: int64
        type: integer
    type: object
  payloads.ApprovalAction:
    enum:
    - approve
    - deny
    type: string
    x-enum-varnames:
    - ApprovalApprove
    - ApprovalDeny
  payloads.ApprovalStatus:
    enum:
    - pending
    - approved
    - denied
    type: string
    x-enum-varnames:
    - ApprovalPending
    - ApprovalApproved
    - ApprovalDenied
  payloads.RequestConvertMultiple:
    properties:
      convertTo:
        items:
          type: string
        type: array
      files:
        items:
          $ref: '#/definitions/multipart.FileHeader'
        type: array
    required:
    - convertTo
    - files
    type: object
  payloads.RequestConvertOne:
    properties:
      convertTo:
        type: string
      file:
        $ref: '#/definitions/multipart.FileHeader'
    required:
    - convertTo
    - file
    type: object
  payloads.RequestCreateNote:
    properties:
      content:
        type: string
      title:
        type: string
      visibility:
        $ref: '#/definitions/models.NoteVisibility'
    required:
    - content
    - title
    - visibility
    type: object
  payloads.RequestCreateShare:
    properties:
      adminUsers:
        example:
        - root
        items:
          type: string
        minItems: 1
        type: array
      browsable:
        allOf:
        - $ref: '#/definitions/models.SambaBool'
        example: "yes"
      name:
        example: apache_source
        type: string
      path:
        example: /var/www/html
        type: string
      permissions:
        example:
        - 7
        - 7
        - 7
        items:
          type: integer
        type: array
      readOnly:
        allOf:
        - $ref: '#/definitions/models.SambaBool'
        example: "no"
      validUsers:
        example:
        - guest
        - chesta
        items:
          type: string
        minItems: 1
        type: array
    required:
    - adminUsers
    - browsable
    - name
    - path
    - permissions
    - readOnly
    - validUsers
    type: object
  payloads.RequestDeleteManyNote:
    properties:
      ids:
        items:
          type: string
        minItems: 1
        type: array
    required:
    - ids
    type: object
  payloads.RequestGetOneNote:
    properties:
      id:
        type: string
    required:
    - id
    type: object
  payloads.RequestRestoreManyNote:
    properties:
      ids:
        items:
          type: string
        minItems: 1
        type: array
      sort:
        allOf:
        - $ref: '#/definitions/models.Sort'
        description: default = desc
    required:
    - ids
    type: object
  payloads.RequestSignIn:
    properties:
      identifier:
        description: email or username
        example: chesta_ardiona
        type: string
      password:
        example: YourPassword123
        type: string
      rememberMe:
        type: boolean
    required:
    - identifier
    - password
    type: object
  payloads.RequestSignUp:
    properties:
      email:
        example: chestaardi4@gmail.com
        type: string
      password:
        example: YourPassword123
        type: string
      username:
        example: chesta_ardiona
        type: string
    required:
    - email
    - password
    - username
    type: object
  payloads.RequestSignUpApproval:
    properties:
      action:
        $ref: '#/definitions/payloads.ApprovalAction'
      identifier:
        type: string
    required:
    - action
    - identifier
    type: object
  payloads.RequestSignUpApprovalStatus:
    properties:
      email:
        type: string
    required:
    - email
    type: object
  payloads.ResponseGetUser:
    properties:
      createdAt:
        example: 2006-01-02T15:04:05Z07:00
        type: string
      email:
        example: chestaardi4@gmail.com
        type: string
      id:
        example: 479b5b5f-81b1-4669-91a5-b5bf69e597c6
        type: string
      updatedAt:
        example: 2006-01-02T15:04:05Z07:00
        type: string
      username:
        example: chesta_ardiona
        type: string
    type: object
  payloads.ResponseSignUpApprovalStatus:
    properties:
      email:
        type: string
      status:
        $ref: '#/definitions/payloads.ApprovalStatus'
      username:
        type: string
    type: object
  reply.ErrorPayload:
    properties:
      code:
        description: Machine-readable error code
        type: string
      details:
        description: Optional debug details
        type: string
      fields:
        allOf:
        - $ref: '#/definitions/reply.FieldsError'
        description: Fields causing the error (if any)
      message:
        description: Human-readable message
        type: string
    type: object
  reply.FieldsError:
    additionalProperties:
      type: string
    type: object
  replylib.CodeError:
    enum:
    - NOT_FOUND
    - SERVER_ERROR
    - BAD_REQUEST
    - BAD_GATEWAY
    - UNAUTHORIZED
    - CONFLICT
    - FORBIDDEN
    - UNPROCESSABLE_ENTITY
    - TOO_MANY_REQUESTS
    - SERVICE_UNAVAILABLE
    - GATEWAY_TIMEOUT
    - METHOD_NOT_ALLOWED
    - NOT_ACCEPTABLE
    - REQUEST_TIMEOUT
    - PAYLOAD_TOO_LARGE
    - UNSUPPORTED_MEDIA_TYPE
    - GONE
    - NOT_IMPLEMENTED
    type: string
    x-enum-varnames:
    - SwagCodeNotFound
    - SwagCodeServerError
    - SwagCodeBadRequest
    - SwagCodeBadGateway
    - SwagCodeUnauthorized
    - SwagCodeConflict
    - SwagCodeForbidden
    - SwagCodeUnprocessableEntity
    - SwagCodeTooManyRequests
    - SwagCodeServiceUnavailable
    - SwagCodeGatewayTimeout
    - SwagCodeMethodNotAllowed
    - SwagCodeNotAcceptable
    - SwagCodeRequestTimeout
    - SwagCodePayloadTooLarge
    - SwagCodeUnsupportedMedia
    - SwagCodeGone
    - SwagCodeNotImplemented
  replylib.Envelope:
    properties:
      data: {}
      meta:
        $ref: '#/definitions/replylib.Meta'
    type: object
  replylib.Meta:
    properties:
      debug: {}
      information:
        type: string
      pagination:
        $ref: '#/definitions/replylib.Pagination'
      status:
        allOf:
        - $ref: '#/definitions/replylib.status'
        description: SUCCESS or ERROR
        example: SUCCESS
      timestamp:
        example: 2006-01-02T15:04:05Z07:00
        type: string
      tokens:
        additionalProperties:
          type: string
        type: object
    type: object
  replylib.Pagination:
    properties:
      current:
        description: current offset
        type: integer
      hasNext:
        description: true if data more than replied
        type: boolean
      next:
        description: if hasHext is false, next is 0
        type: integer
    type: object
  replylib.status:
    enum:
    - SUCCESS
    - ERROR
    type: string
    x-enum-varnames:
    - Success
    - Error
  textproto.MIMEHeader:
    additionalProperties:
      items:
        type: string
      type: array
    type: object
host: localhost:8080
info:
  contact: {}
  description: This is an API used for manages home server (ubuntu/debian).
  title: Homedy API
  version: "1.0"
paths:
  /auth/me:
    get:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.User'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Get signed in account information
      tags:
      - auth
  /auth/signin:
    post:
      consumes:
      - application/json
      parameters:
      - description: sign in identity
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestSignIn'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.User'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Sign in onto existing account
      tags:
      - auth
  /auth/signout:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/replylib.Envelope'
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Sign out account for one device (request device). Have to sign in first
      tags:
      - auth
  /auth/signup:
    post:
      consumes:
      - application/json
      description: Create new account and ask [config.MAIL_OWNER] to register approval,
        user will be notified if account successfully review
      parameters:
      - description: data of new account
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestSignUp'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/replylib.Envelope'
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Creates new account
      tags:
      - auth
  /auth/signup/approval:
    patch:
      consumes:
      - application/json
      description: Owner only allow or deny sign up request. Identified by app secret
      parameters:
      - description: app secret authentication for access
        in: header
        name: X-APP-SECRET
        required: true
        type: string
      - description: data of sign up request
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestSignUpApproval'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.User'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Allow or deny sign up request
      tags:
      - auth
  /auth/signup/approval-status:
    get:
      consumes:
      - application/json
      parameters:
      - description: identity to check status
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestSignUpApprovalStatus'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/payloads.ResponseSignUpApprovalStatus'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Check sign up status
      tags:
      - auth
  /backup:
    post:
      parameters:
      - description: app secret authentication for access
        in: header
        name: X-APP-SECRET
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/replylib.Envelope'
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Backup samba configuration and shares and replace previous backup
      tags:
      - folder-sharing
  /config:
    get:
      parameters:
      - description: app secret authentication for access
        in: header
        name: X-APP-SECRET
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.ShareMap'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Get samba configuration
      tags:
      - folder-sharing
    put:
      consumes:
      - application/json
      parameters:
      - description: app secret authentication for access
        in: header
        name: X-APP-SECRET
        required: true
        type: string
      - description: updated samba config
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/models.ShareMap'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.ShareMap'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Update samba configuration
      tags:
      - folder-sharing
  /convert/multiple:
    post:
      consumes:
      - multipart/form-data
      parameters:
      - description: files and convert target
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestConvertMultiple'
      produces:
      - application/json
      - application/zip
      responses:
        "200":
          description: 'File type: zip'
          schema:
            type: file
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Convert multiple files and zip converted
      tags:
      - converter
  /convert/single:
    post:
      consumes:
      - multipart/form-data
      parameters:
      - description: file and convert target
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestConvertOne'
      produces:
      - application/json
      - application/octet-stream
      responses:
        "200":
          description: 'File type: convert target'
          schema:
            type: file
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Convert one file and response converted file
      tags:
      - converter
  /notes:
    delete:
      parameters:
      - description: param of notes` identification
        in: body
        name: param
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestDeleteManyNote'
      produces:
      - application/json
      responses:
        "200":
          description: data is null
          schema:
            $ref: '#/definitions/replylib.Envelope'
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Soft delete one client's existing notes
      tags:
      - note
    get:
      parameters:
      - in: query
        minimum: 0
        name: offset
        type: integer
      - in: query
        name: recycled
        type: boolean
      - description: default = desc
        enum:
        - asc
        - desc
        in: query
        name: sort
        type: string
        x-enum-varnames:
        - ASC
        - DESC
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  items:
                    $ref: '#/definitions/models.Note'
                  type: array
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                      meta:
                        $ref: '#/definitions/replylib.Pagination'
                    type: object
              type: object
      summary: Get all client's existing notes with query
      tags:
      - note
    post:
      consumes:
      - application/json
      parameters:
      - description: new note data
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestCreateNote'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Note'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Create one new note
      tags:
      - note
  /notes/{id}:
    delete:
      parameters:
      - in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: deleted id in server
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.BaseID'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Soft delete one client's existing note
      tags:
      - note
    get:
      parameters:
      - in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Note'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Get client's existing notes by id
      tags:
      - note
    put:
      consumes:
      - application/json
      parameters:
      - description: updated note
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestGetOneNote'
      - example: 479b5b5f-81b1-4669-91a5-b5bf69e597c6
        in: path
        name: id
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: updated note from server
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Note'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Update client's existing note
      tags:
      - note
  /notes/restore:
    patch:
      parameters:
      - description: param of notes` identification
        in: body
        name: param
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestRestoreManyNote'
      produces:
      - application/json
      responses:
        "200":
          description: restored notes data
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  items:
                    $ref: '#/definitions/models.Note'
                  type: array
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Restore existing share by name
      tags:
      - note
  /notes/restore/{id}:
    patch:
      parameters:
      - in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: restored note data
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Note'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Restore one client's existing note
      tags:
      - note
  /oauth/github:
    get:
      responses:
        "307":
          description: Temporary Redirect
      summary: Redirect to github oauth
      tags:
      - OAuth
  /oauth/github/callback:
    get:
      parameters:
      - in: query
        name: code
        required: true
        type: string
      - in: query
        name: state
        required: true
        type: string
      produces:
      - application/json
      responses:
        "307":
          description: Temporary Redirect
      summary: Callback of github oauth
      tags:
      - OAuth
  /restore:
    post:
      parameters:
      - description: app secret authentication for access
        in: header
        name: X-APP-SECRET
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: all existing shares
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Shares'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Restore samba configuration and shares from latest backup
      tags:
      - folder-sharing
  /samba:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Shares'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Get all existing shares
      tags:
      - folder-sharing
    post:
      consumes:
      - application/json
      parameters:
      - description: new folder share config
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/payloads.RequestCreateShare'
      produces:
      - application/json
      responses:
        "201":
          description: all existing shares
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Shares'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Create new folder share
      tags:
      - folder-sharing
  /samba/{name}:
    delete:
      parameters:
      - example: apache_source
        in: path
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: all existing shares
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Shares'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Delete existing share by name
      tags:
      - folder-sharing
    get:
      parameters:
      - example: apache_source
        in: path
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Share'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Get existing share by name
      tags:
      - folder-sharing
    put:
      consumes:
      - application/json
      parameters:
      - description: updated folder share config
        in: body
        name: payload
        required: true
        schema:
          $ref: '#/definitions/models.Share'
      - example: apache_source
        in: path
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: all existing shares
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/models.Shares'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Update existing share by name
      tags:
      - folder-sharing
  /users/{id}:
    get:
      parameters:
      - in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  $ref: '#/definitions/payloads.ResponseGetUser'
              type: object
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Get existing user by id
      tags:
      - user
  /ws/terminal:
    get:
      parameters:
      - description: app secret authentication for access
        in: query
        name: app_secret
        required: true
        type: string
      produces:
      - application/json
      responses:
        default:
          description: ""
          schema:
            allOf:
            - $ref: '#/definitions/replylib.Envelope'
            - properties:
                data:
                  allOf:
                  - $ref: '#/definitions/reply.ErrorPayload'
                  - properties:
                      code:
                        $ref: '#/definitions/replylib.CodeError'
                    type: object
              type: object
      summary: Websocket to access terminal
      tags:
      - terminal
swagger: "2.0"
